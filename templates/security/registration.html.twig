{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    <h4>Inscription</h4>

    {{ form_start(formUser) }}
   
        <div class="row">
            <div class="col-6">
                {{ form(formUser) }}
            </div>
        </div>
        
    {{ form_end(formUser) }}


{% endblock %}